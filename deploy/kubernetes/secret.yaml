# Replace this with a real secret, or better, a SealedSecret / ExternalSecret.
# The value must be byte-identical to RomM's STREAMING_BROKER_SECRET.
apiVersion: v1
kind: Secret
metadata:
  name: flycast-broker
type: Opaque
stringData:
  broker-secret: CHANGE-ME
